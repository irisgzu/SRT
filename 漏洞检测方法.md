因为哪个是英文的 这个是我当时看的时候选取一些关键部分 翻译成中文的
然后现在的有关发展也是从规则和数据驱动转到了 基于LLM的语义推理的框架

# 传统漏洞检测方法

传统方法主要包括静态分析和动态分析两大类。静态分析（如规则匹配、符号执行、污点分析）在不执行程序的情况下检查源代码结构、语法和数据流，能全面覆盖代码但误报率较高，且受Rice定理的理论限制；动态分析（如模糊测试、动态污点追踪）则在程序运行时监控行为，可发现运行时特有的漏洞（如内存损坏、竞态条件），但代码覆盖率有限，依赖测试用例质量。传统方法依赖人工规则和启发式策略，在可扩展性和对新型漏洞的适应性上存在明显不足。

# 机器学习（ML）方法

早期ML方法依赖专家手工设计代码特征，如代码行数、圈复杂度、API调用序列等软件度量指标，再输入到支持向量机、随机森林、决策树等传统分类器中进行训练。这类方法训练速度快、模型可解释性相对较好，但特征工程高度依赖领域经验，难以捕捉代码的深层语义和上下文关系，且面对真实世界复杂代码时泛化能力有限。论文统计显示，随机森林和SVM是该领域最常采用的ML模型。

# 深度学习（DL）方法

DL方法能够自动从代码中学习特征表示，免去了繁琐的手动特征工程。常用模型包括CNN（用于从token序列中提取层次化模式）、LSTM/BiLSTM（捕捉代码的长期顺序依赖）以及GNN及其变体（利用AST、CFG、PDG等图结构学习控制流和数据流关系）。DL方法在基准数据集上对SQL注入、XSS等Web漏洞的检测准确率可达93%–99.9%，但论文指出其存在严重的泛化困境——在真实世界软件项目中的性能会显著下降，且注意力机制被证实能有效提升检测效果，而迁移学习带来的增益有限。

# 大语言模型（LLM）方法

LLM（如BERT、CodeBERT、CodeT5、GPT系列、LLaMA等）在海量代码与文本语料上预训练，展现出强大的代码语义理解和上下文推理能力，能够通过微调或提示工程直接用于漏洞检测、解释与修复建议生成。论文将其分为编码器型、编码器-解码器型和仅解码器型三类架构。相比于传统方法，LLM在F1分数和召回率上表现更优，但面临幻觉现象、高计算成本、可解释性差、跨语言泛化能力不足等挑战。参数高效微调和神经符号混合方法是当前提升其实用性的重要研究方向。