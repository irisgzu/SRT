集成
[Log into your Pentest-Tools.com account](https://app.pentest-tools.com/signup)

XSS
[https://github.com/s0md3v/XSStrike](https://github.com/s0md3v/XSStrike)
[XSS 渗透测试必备工具！XSStrike 实战演示！-CSDN博客](https://blog.csdn.net/Brad_PiTt7/article/details/146321894)


形式
	命令行
	GUI


## Burp Suite
要点总结
一次扫描
	选择目标站点
	扫描方式的配置等
查看扫描结果：Dashboard/Target-Issue



Burp Suite 的自动化漏洞扫描功能主要集成在 **Dashboard（仪表盘）** 和 **Target（目标）** 模块中，其核心引擎是 **Burp Scanner**。

### 1. 核心扫描入口

* **Dashboard -> New Scan**：
这是最常用的入口。你可以手动输入 URL，配置扫描范围、抓取（Crawl）深度以及审计（Audit）策略。你可以选择“Crawl and Audit”（爬取并审计）或仅“Audit”（审计已有记录）。
* **Target -> Site Map -> 右键点击目标**：
在 Site Map 中选中特定的域名、目录或请求，右键选择 **Scan**。这种方式更具针对性，可以避免扫描无关的资产。
* **Proxy -> HTTP History -> 右键点击请求**：
如果你在截获流量时发现某个请求可能存在 SQLi 或 XSS（例如带有明显的 `id=` 或 `search=` 参数），可以直接右键选择 **Scan**，对该单一请求进行定向审计。

---

### 2. 功能配置与策略

在启动扫描时，可以通过以下配置优化对 SQLi 和 XSS 的探测：

* **Scan Configuration (扫描配置)**：
* **Audit Checks**：在设置中，你可以勾选特定的检查项。例如，只勾选 `Input vector - SQL injection` 或 `Cross-site scripting` 来加快扫描速度。
* **Fuzzing Strategy**：可以调整 Payload 的强度，决定是进行轻量级探测还是深度的多变体绕过测试。


* **Insertion Points (插入点)**：
Scanner 会自动识别 URL 参数、Body 参数、Cookie、Header（如 User-Agent, Referer）等。你可以在配置中自定义这些插入点。

---

### 3. 结果查看

* **Issue Activity (漏洞活动)**：
扫描发现的所有潜在风险（Issue）都会实时显示在 Dashboard 的 Issue 窗口中。
* **Advisory (漏洞详情)**：
点击具体漏洞，右侧会显示 **Request**（发送的恶意 Payload）和 **Response**（服务器的异常响应），并附带详细的漏洞原理、修复建议以及在该请求中发现漏洞的确切位置。

### 4. 协作插件 (BApp Store)

虽然内置 Scanner 已经很强大，但通过 **Extensions -> BApp Store** 安装以下插件可以进一步增强其扫描能力：

* **ActiveScan++**：扩展内置扫描器的功能，自动检测更多复杂的漏洞变体。
* **BurpJS Linker**：增强对 JavaScript 文件的解析，从而发现更多隐藏的 XSS 入口。
* **Param Miner**：自动寻找隐藏参数，这些参数往往是 SQLi 和 XSS 的盲区。

> **注意**：漏洞扫描功能（Scanner）仅在 **Burp Suite Professional** 和 **Enterprise** 版本中可用。社区版（Community Edition）仅提供基本的代理、截获和重放功能，不支持自动化审计。

## https://app.pentest-tools.com/


Dashboard：统筹管理，攻击面总结，漏洞数量图等

一次扫描
	指定一个扫描的网址
	获取扫描结果
		按漏洞的威胁等级和类型进行了分类
		支持导出为pdf等格式



# 漏扫工具推荐
针对 SQL 注入（SQLi）和跨站脚本攻击（XSS）的漏洞扫描工具，以下是行业公认的生产级选择：

## 1. 综合型动态应用安全测试 (DAST)

这些工具通过模拟黑客攻击来发现运行中的 Web 应用漏洞。

* **Burp Suite Professional / Enterprise**
* **特点**：业界事实上的标准。其 **Burp Scanner** 引擎对 XSS 和 SQLi 的检测极其精准，支持复杂的交互式扫描。
* **链接**：[https://portswigger.net/burp](https://portswigger.net/burp)


* **OWASP ZAP (Zaproxy)**
* **特点**：最流行的开源免费工具。支持自动扫描和 API 驱动，适合集成到 CI/CD 流水线中。
* **链接**：[https://www.zaproxy.org/](https://www.zaproxy.org/)


* **Acunetix by Invicti**
* **特点**：扫描速度快，拥有极高的 SQLi 和 XSS 检出率，专门针对单页应用 (SPA) 进行了优化。
* **链接**：[https://www.acunetix.com/](https://www.acunetix.com/)



---

## 2. 专项自动化工具

针对特定漏洞类型深度优化的工具。

* **sqlmap (专注 SQLi)**
* **特点**：开源命令行工具，自动化探测及利用 SQL 注入漏洞的极致之作。支持几乎所有主流数据库。
* **链接**：[https://sqlmap.org/](https://sqlmap.org/)


* **XSStrike (专注 XSS)**
* **特点**：高级 XSS 检测工具，配备强大的解析引擎，能够生成定制化的 Payload 并尝试绕过 WAF。
* **链接**：[https://github.com/s0md3v/XSStrike](https://github.com/s0md3v/XSStrike)



---

## 3. 现代扫描框架与资产巡航

适合大规模内网或云环境的快速资产扫描。

* **Nuclei**
* **特点**：基于模板的极速漏洞扫描器。社区维护了大量的 SQLi 和 XSS 检测模板（YAML 格式），高度可定制。
* **链接**：[https://github.com/projectdiscovery/nuclei](https://github.com/projectdiscovery/nuclei)


* **Goby**
* **特点**：新一代网络安全测试工具，拥有出色的资产识别能力和可视化界面，内置多种高危漏洞扫描插件。
* **链接**：[https://gobies.org/](https://gobies.org/)



---

## 4. 静态分析 (SAST)

在代码未运行前，通过分析源代码逻辑发现潜在漏洞。

* **Semgrep**
* **特点**：轻量级静态分析工具，规则简单易懂。非常适合在开发阶段拦截未经过滤的输入点。
* **链接**：[https://semgrep.dev/](https://semgrep.dev/)


* **SonarQube**
* **特点**：企业级代码质量与安全扫描平台，支持多种语言的 SQLi 和 XSS 污染链分析。
* **链接**：[https://www.sonarsource.com/products/sonarqube/](https://www.sonarsource.com/products/sonarqube/)



| 工具 | 类型 | 适用场景 | 费用 |
| --- | --- | --- | --- |
| **Burp Suite** | DAST | 手动渗透/自动化扫描 | 付费 |
| **ZAP** | DAST | 开发/测试环节集成 | 开源 |
| **sqlmap** | 专项 | 深度 SQLi 探测 | 开源 |
| **Nuclei** | 扫描框架 | 大规模资产快速巡检 | 开源 |

# 漏扫工具推荐（AI）

结合 AI 技术的漏洞扫描工具通过机器学习（ML）和大型语言模型（LLM）来降低误报率，并自动生成针对性的绕过 Payload。以下是目前市面上主流的“AI+安全”漏扫工具：

### 1. 综合性 AI 驱动平台

* **Burp Suite (BApp Extensions + AI Plugins)**
* **AI 结合点**：PortSwigger 正在将机器学习引入其扫描引擎。目前社区已有如 **Burp-GPT** 等插件，利用 OpenAI API 分析流量中的复杂 XSS 和 SQLi 逻辑。
* **场景**：手动渗透与自动审计结合。


* **Invicti (前 Acunetix/Netsparker)**
* **AI 结合点**：利用 **Predictive Analysis** 模拟人类渗透测试者的思维，识别传统 DAST 难以发现的逻辑漏洞和复杂注入点。
* **场景**：企业级大规模 Web 资产自动扫描。



### 2. 智能化 SAST (静态代码分析)

* **Snyk (DeepCode AI)**
* **AI 结合点**：基于 DeepCode 技术，使用大型逻辑库和机器学习模型进行代码扫描。它能识别出污染链（Taint Analysis），精准定位从未经过滤的输入点到数据库执行点（SQLi）或前端渲染点（XSS）的路径。
* **特点**：极低的误报率，支持修复建议。


* **GitHub Advanced Security (CodeQL + Copilot Autofix)**
* **AI 结合点**：CodeQL 提供强大的语义分析，配合 Copilot AI 可以自动生成修复 SQLi 和 XSS 的代码补丁。
* **场景**：DevSecOps 流程中的实时防护。



### 3. 创新型 AI 扫描器

* **Escape.tech**
* **AI 结合点**：专注于 API（GraphQL/REST）安全。利用 AI 生成流量模型，在没有文档的情况下自动探索业务逻辑并尝试注入攻击。
* **场景**：现代 Web 应用与移动端后端接口扫描。


* **Nuclei (配合 AI 模板生成)**
* **AI 结合点**：虽然 Nuclei 本身是引擎，但目前主流做法是利用 LLM（如 GPT-4）编写、调试自定义的 YAML 探测模板，针对特定的 WAF 环境生成变体 Payload。



### 4. 专业漏洞验证助手

* **PentestGPT**
* **AI 结合点**：基于 GPT-4 的渗透测试辅助工具。它可以分析漏扫工具的原始输出，引导用户进行后续的 SQLi/XSS 手动验证。
* **场景**：辅助渗透测试流程，解决“工具发现但无法利用”的问题。



---

### 工具选型建议

| 需求场景 | 推荐工具 | AI 核心优势 |
| --- | --- | --- |
| **代码阶段拦截** | **Snyk / SonarQube** | 污染链追踪，准确识别 SQLi/XSS 源头。 |
| **黑盒动态扫描** | **Invicti / Burp-GPT** | 自动绕过 WAF，减少无效告警。 |
| **API 安全测试** | **Escape** | 自动发现隐藏的 API 端点与注入风险。 |
| **自动化利用验证** | **PentestGPT** | 自动化规划攻击路径，生成验证 PoC。 |